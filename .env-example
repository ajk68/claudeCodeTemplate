# Database connection (if using database features)
CARV_DB_URL=postgresql://

# AI API Keys - Add the ones you need
OPENAI_API_KEY=                # Optional, for GPT models
PERPLEXITY_API_KEY=           # For web search via MCP server
ANTHROPIC_API_KEY=            # For AI delegation commands
GOOGLE_API_KEY=               # For Gemini models (alternative name)
LLM_GEMINI_KEY=               # For Gemini models (alternative name)
OPENROUTER_API_KEY=           # Optional, for OpenRouter models

# GitHub token for repository operations
GH_TOKEN=                     # For GitHub MCP server and git operations
